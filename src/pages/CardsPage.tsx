import { IonPage, IonHeader, IonToolbar, IonTitle, IonContent } from "@ionic/react";

export default function CardsPage() {
  return (
    <IonPage>
      <IonHeader>
        <IonToolbar>
          <IonTitle>活動卡片收集</IonTitle>
        </IonToolbar>
      </IonHeader>
      <IonContent className="ion-padding">
        {/* TODO: card collection UI */}
      </IonContent>
    </IonPage>
  );
}
